1. There is a logo in @../../../zive-teplice-frontend/public/img/logo.svg or logo.png. - We need to add the logo to the header. Find some way to add it but not to break the layout and both the light and dark theme. Tho logo should fit nicely in the header and not be too big. The logo should be a link to the home page. The logo should be visible on all pages, including the admin pages. The logo should be responsive and look good on mobile devices. The logo should have alt text for accessibility.
2. Also the logo should be visible in browser tabs (favicon). We can use the same logo.svg or logo.png for that. Make sure to add the favicon to the head of the document and test it on different browsers and devices. The favicon should be 32x32 pixels and in .ico format.
3. The light theme and light theme background color is too white, could you find some shade of grey that is not too dark and not too light, and use it as the background color for the light theme. The background color should be consistent across all pages and components, including the header, footer, and main content area. The background color should also have good contrast with the text and other elements on the page.
4. The user registration form should have a Popis field that is required and has a minimum length of 10 characters and maximul of 300 characters.
5. Also when the user register it should automatically send a request to participate in the current event. The request should be created with the status "pending" and should be linked to the user and the current event.
6. In The header we should include links for social media (Facebook and INstagram), could you find/create same icons for them that would fit our theme and layout nicely. 
7. Also the Instagram link is static but the Facebook link change for every event so it should be configurable and stored in the database. Maybe do it for both links and store them in the database as part of the app settings. The links should be editable by an admin user and should be displayed in the header on all pages. The links should open in a new tab and have appropriate alt text for accessibility.
8. Also the gallery name has maximum length of 15 characters. Could you increase that to 30.

